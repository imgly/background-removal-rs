name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: ${{ matrix.rust }}

    - name: Cache cargo registry
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-

    - name: Install FFmpeg (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg libavcodec-dev libavformat-dev libavfilter-dev libavdevice-dev

    - name: Install FFmpeg (macOS)
      if: matrix.os == 'macos-latest'
      run: brew install ffmpeg

    - name: Install FFmpeg (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        choco install ffmpeg-full
        choco install pkgconfiglite
        echo "C:\ProgramData\chocolatey\lib\ffmpeg\tools\ffmpeg\bin" >> $env:GITHUB_PATH
      env:
        FFMPEG_DIR: C:\ProgramData\chocolatey\lib\ffmpeg-full\tools\ffmpeg

    - name: Check
      run: cargo check --verbose
      if: matrix.os != 'windows-latest'

    - name: Check (Windows without video)
      if: matrix.os == 'windows-latest'
      run: cargo check --verbose --no-default-features --features "onnx,tract,cli,webp-support"

    - name: Build
      run: cargo build --verbose
      if: matrix.os != 'windows-latest'

    - name: Build (Windows without video)
      if: matrix.os == 'windows-latest'
      run: cargo build --verbose --no-default-features --features "onnx,tract,cli,webp-support"

    - name: Test
      run: cargo test --verbose
      if: matrix.os != 'windows-latest'

    - name: Test (Windows without video)
      if: matrix.os == 'windows-latest'
      run: cargo test --verbose --no-default-features --features "onnx,tract,cli,webp-support"